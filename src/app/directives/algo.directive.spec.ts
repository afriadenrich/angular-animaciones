import { AlgoDirective } from './algo.directive';

describe('AlgoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlgoDirective();
    expect(directive).toBeTruthy();
  });
});
